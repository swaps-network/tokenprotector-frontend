/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { limitTo } from './limit-to.pipe';

describe('Pipe: LimitToe', () => {
  it('create an instance', () => {
    let pipe = new limitTo();
    expect(pipe).toBeTruthy();
  });
});
